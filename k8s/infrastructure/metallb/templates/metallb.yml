---
apiVersion: metallb.io/v1beta1
kind: IPAddressPool
metadata:
  name: homelab-pool
  namespace: "{{ .Release.Namespace }}"
spec:
  addresses:  
    {{- .Values.homelab.addresses | toYaml | nindent 4 }}
---
apiVersion: metallb.io/v1beta1
kind: L2Advertisement
metadata:
  name: l2ad
  namespace: "{{ .Release.Namespace }}"
