---
apiVersion: v1
kind: Secret
metadata:
  name: argo-repo-main
  namespace: "{{ .Release.Namespace }}"
  labels:
    argocd.argoproj.io/secret-type: repository
stringData:
  type: git
  url: "{{ .Values.selfapp.repo }}"
  sshPrivateKey: {{ .Values.selfapp.sshkey | quote }}