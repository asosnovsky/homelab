---
apiVersion: v1
kind: Service
metadata:
  name: ldap
  labels:
    app.kubernetes.io/name: ldap
spec:
  type: ClusterIP
  ports:
    - name: tcp
      port: 1389
      targetPort: tcp
  selector:
    app.kubernetes.io/name: ldap
