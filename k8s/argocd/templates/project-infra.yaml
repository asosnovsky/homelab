{{- if .Values.selfapp.enabled -}}
---
apiVersion: argoproj.io/v1alpha1
kind: AppProject
metadata:
  name: infra
  namespace: "{{ .Release.Namespace }}"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  description: infra
  sourceRepos:
    - "git@github.com:{{ .Values.selfapp.repo }}.git"
  destinations:
    - server: "*"
      namespace: '*'
      name: '*'
  clusterResourceWhitelist:
    - group: "*"
      kind: "*"
{{- end -}}