---
apiVersion: traefik.containo.us/v1alpha1
kind: Middleware
metadata:
  name: nextcloud
  namespace: apps-nextcloud
spec:
  chain:
    middlewares:
      - name: https-redirect
        namespace: "{{ .Release.Namespace }}"
      - name: apps-nextcloud-well-known
      - name: apps-nextcloud-well-known-web-finger
      - name: strict-transport
        namespace: "{{ .Release.Namespace }}"

---
apiVersion: traefik.containo.us/v1alpha1
kind: Middleware
metadata:
  name: apps-nextcloud-well-known
  namespace: apps-nextcloud
spec:
  redirectRegex:
    permanent: true
    regex: "https://(.*)/.well-known/(?:card|cal)dav"
    replacement: "https://${1}/remote.php/dav"

---
apiVersion: traefik.containo.us/v1alpha1
kind: Middleware
metadata:
  name: apps-nextcloud-well-known-web-finger
  namespace: apps-nextcloud
spec:
  redirectRegex:
    permanent: true
    regex: "https://(.*)/.well-known/webfinger"
    replacement: "https://${1}/public.php?service=webfinger"
