{{  if .Values.global.tls.enabled  }}
---
apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
    name: {{ .Values.global.tls.issuer }}
spec:
    acme:
        server: {{ .Values.global.tls.acmeServer }}
        skipTLSVerify: {{ .Values.global.tls.skipTLSVerify }}
        email: {{ .Values.global.email }}
        privateKeySecretRef:
            name: {{ .Values.global.tls.issuer }}
        solvers:
            -   http01:
                    ingress:
                        class: traefik

{{ if .Values.cloudflare.enabled }}
---
apiVersion: cert-manager.io/v1
kind: Issuer
metadata:
  name: cloudflare
spec:
  acme:
    server: {{ .Values.global.tls.acmeServer }}
    skipTLSVerify: {{ .Values.global.tls.skipTLSVerify }}
    email: {{ .Values.global.email }}
    privateKeySecretRef:
        name: cloudflare
    solvers:
        -   dns01:
                cloudflare:
                    apiTokenSecretRef:
                        name: {{ .Values.cloudflare.tokenSecretName }}
                        key: {{ .Values.cloudflare.tokenSecretKey }}
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: cloudflare-wildcard.{{ .Values.global.domain }}
spec:
  secretName: cloudflare-wildcard.{{ .Values.global.domain }}
  issuerRef:
    name: cloudflare
    kind: Issuer
  commonName: '*.{{ .Values.global.domain }}'
  dnsNames:
    -   "*.{{ .Values.global.domain }}"
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: cloudflare-wildcard.core.{{ .Values.global.domain }}
spec:
  secretName: cloudflare-wildcard.core.{{ .Values.global.domain }}
  issuerRef:
    name: cloudflare
    kind: Issuer
  commonName: '*.core.{{ .Values.global.domain }}'
  dnsNames:
    -   "*.core.{{ .Values.global.domain }}"
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: cloudflare-wildcard.vh.{{ .Values.global.domain }}
spec:
  secretName: cloudflare-wildcard.vh.{{ .Values.global.domain }}
  issuerRef:
    name: cloudflare
    kind: Issuer
  commonName: '*.vh.{{ .Values.global.domain }}'
  dnsNames:
    -   "*.vh.{{ .Values.global.domain }}"

{{ end }}
{{ end }}