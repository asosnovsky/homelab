nextcloud:
  nextcloud:
    configs:
      proxy.config.php: |-
        <?php
        $CONFIG = array (
          'trusted_proxies' => array(
            0 => '127.0.0.1',
            1 => '10.0.0.0/8',
          ),
          'overwritehost'     => 'nextcloud.home.sosnovsky.ca',
          'overwriteprotocol' => 'https',
          'forwarded_for_headers' => array('HTTP_X_FORWARDED_FOR'),
        );
