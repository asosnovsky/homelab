---
apiVersion: metallb.io/v1beta1
kind: IPAddressPool
metadata:
  name: homelab-pool
  namespace: "{{ .Values.ns.metallb }}"
spec:
  addresses:
    - 10.0.100.1-10.0.100.255
---
apiVersion: metallb.io/v1beta1
kind: L2Advertisement
metadata:
  name: l2ad
  namespace: "{{ .Values.ns.metallb }}"
