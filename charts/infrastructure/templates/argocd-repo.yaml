---
apiVersion: v1
kind: Secret
metadata:
  labels:
    argocd.argoproj.io/secret-type: repository
  name: homelab
  namespace: "{{ .Values.ns.argocd }}"
type: Opaque
stringData:
    name: homelab
    type: git
    username: git
    url: {{ .Values.gitops.repo }}
data:
  sshPrivateKey: "{{ .Values.gitops.sshkey | b64enc }}"
